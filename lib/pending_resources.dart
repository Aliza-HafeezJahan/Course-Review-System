import 'package:flutter/material.dart';



class PendingResourcesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pending Resources'),
      ),
      body: Center(
        child: Text('This is the pending resources page'),
      ),
    );
  }
}